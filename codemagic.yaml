workflows:
  ios-workflow:
    name: iOS Build
    max_build_duration: 60

    environment:
      xcode: latest
      node: latest

    scripts:
      - npm install
      - npx cap sync ios

    artifacts:
      - ios/App/build/**/*.ipa
